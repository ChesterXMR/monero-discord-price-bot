{
  "name": "monero-price-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put DISCORD_TOKEN"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
